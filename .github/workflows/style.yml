name: Style

on:
  push:
    branches: [ max/cpu_tests ]
  pull_request:

jobs:
  base:
    uses: .github/workflows/setup-dev-workflow.yml
  style:
    needs: base
    runs-on: ubuntu-latest
    steps:
      - run: make style