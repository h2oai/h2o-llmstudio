name: Test

on:
  workflow_call:
  push:
    branches: [ max/cpu_tests ]
  pull_request:

jobs:
  base:
    uses: .github/workflows/setup-dev-workflow.yml
  test:
    runs-on: ubuntu-latest
    steps:
      - run: make test