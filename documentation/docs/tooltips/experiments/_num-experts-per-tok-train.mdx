Number of experts to choose for each token while training. If set to -1 will use the same value as for inference.

It can be useful to set this to higher values than for inference, particularly for distributed training to avoid missing gradients.